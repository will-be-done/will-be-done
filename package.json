{
  "name": "will-be-done",
  "version": "0.1.0",
  "author": "quolpr",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {},
  "scripts": {
    "dev:client": "bun --cwd apps/web run dev",
    "dev:server": "bun --cwd apps/api run dev",
    "ts": "bun run --filter='@will-be-done/web' ts && bun run --filter='@will-be-done/slices' ts && bun run --filter='@will-be-done/api' ts",
    "lint": "bun run --filter='@will-be-done/web' lint && bun run --filter='@will-be-done/slices' lint && bun run --filter='@will-be-done/api' lint"
  },
  "devDependencies": {},
  "overrides": {
    "zod-validation-error": "^4.0.0"
  }
}
